  <div class="row">
    <div class="col-lg-5 col-md-5 col-lg-5 px-5 px-sm-0">
    <h2 style="font-size: 25px; margin-bottom: 20px;">ジャンル編集</h2>
    <%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
      <label>ジャンル名</label>
      <%= f.text_field :name %>
      <%= f.submit '変更を保存', class: "btn btn-success" %>
    <% end %>
    </div>
  </div>