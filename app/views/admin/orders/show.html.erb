<h1>注文履歴詳細</h1>
<%= form_with model: @order, url: admin_items_path, method: :patch do |f| %>
  購入者
  <%= @order.customer.last_name + @order.customer.first_name %>
  <br>
  注文日
  <%= @order.created_at.strftime('%Y/%m/%d') %>
  <br>
  配送先
  <%= @order.order_display %>
  <br>
  支払方法
  <%= @order.return_payment_method %>
  <br>
<% end %>