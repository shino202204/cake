<div class="row">
  <div class="col-lg-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  <h2 style="font-size: 25px; margin-bottom: 20px;">ログイン</h2>
  <p>会員の方はこちらからログイン</p>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field row">
      <span class="col-lg-2"><%= f.label 'メールアドレス' %></span>
      <span>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'sample@example.com' %>
      </span>
    </div>

    <div class="field row">
      <span class="col-lg-2"><%= f.label 'パスワード' %></span>
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div>

    <div class="actions row">
      <span class="col-lg-2 mx-auto"><%= f.submit "ログイン", class: "btn btn-primary" %></span>
    </div>
  <% end %>

  <p>会員登録がお済みでない方</p>
  <div style="display: flex">
    <div>
      <%= render "public/shared/links" %>
    </div>
    <div>
      から新規登録を行なってください。
    </div>
  <div>

  </div>
</div>